int climbStairs(int n) {
        vector<int> t(n+1,-1);
        // return helper(n,t);
        t[0]=1;   //for 0th stair we are considering 1 way
        t[1]=1;
        for (int i=2;i<n+1;i++){
            t[i]=t[i-1]+t[i-2];
        }
        return t[n];
    }
